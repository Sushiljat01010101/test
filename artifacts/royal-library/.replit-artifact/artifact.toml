kind = "web"
previewPath = "/"
title = "Royal Library"
version = "1.0.0"
id = "artifacts/royal-library"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22769

[services.development]
run = "pnpm --filter @workspace/royal-library run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/royal-library", "run", "build" ]
publicDir = "artifacts/royal-library/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22769"
BASE_PATH = "/"
